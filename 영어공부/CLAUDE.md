# 영어공부 프로젝트

## 하네스: 비즈니스 영어 학습 (바이오 R&D PM)

**목표:** 듣기 약점 극복 → 상대방의 말을 알아듣고 유창하게 자기 생각을 말하기. 커리큘럼 강요 없이 사용자가 원하는 실무 문장을 암기하고 다국 악센트로 듣기 훈련.

**트리거:** 영어 학습 관련 요청(영어공부, 듣기 연습, 받아쓰기, 롤플레이, 복습, 문장 추가, 학습 현황 등) 시 `english-study` 스킬을 사용하라. 단순 번역/작문 단건 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-10 | 초기 구성 (에이전트 2, 스킬 6, 시드 문장 5) | 전체 | - |
| 2026-06-10 | 악센트: 미국+영국 4음성으로 시작 | multi-accent-audio | 사용자 선택 (인도/호주 등은 추후 확장) |
| 2026-06-10 | study-record 스킬 추가 (단어장 vocab.json + 누적 이력 study_log.json), 전 모드 기록 연결 | skills/study-record 신설, english-study·listening-drill·roleplay-meeting·review-srs·에이전트 2종 수정 | "공부한 단어/문장 기록해서 이력을 쌓아달라" 요청 |
| 2026-06-10 | 학습 웹앱 추가 (app/server.py + index.html — 받아쓰기·SRS·단어장·이력), sentences.json에 notes(듣기 해설) 필드 신설 | app/ 신설, sentence-pack·english-study 수정 | "cmd 말고 HTML/앱으로 직관적으로 공부하고 싶다" 요청 |
| 2026-06-10 | 앱 UI 개선: 콘텐츠 화면 정중앙 배치, 전체 글자 크기 확대 | app/index.html | 사용자 UI 피드백 |
| 2026-06-10 | 앱 기능 3종 추가: ① 문장 카드 넘기기(문장 공부 탭) ② 앱 내 문장 추가(/api/add_sentence — Claude CLI로 번역·해설 자동 생성 + 오디오 생성) ③ YouGlish 위젯 연동(패턴/문장 실제 발화 영상) | app/server.py, app/index.html | "문장 넘기면서 골라 공부 + 원하는 문장 적으면 발음·해설 + YouGlish 연동" 요청 |
| 2026-06-10 | Netlify 배포 지원: 앱 이중 모드(로컬=서버 API, 온라인=localStorage+브라우저 TTS 폴백), 배포 폴더 빌더(app/build_netlify.py + 배포폴더만들기.bat), 백업 내보내기 버튼 | app/index.html, app/build_netlify.py 신설 | "app.netlify.com에 올려서 사용하고 싶다" 요청 |
| 2026-06-13 | 받아쓰기 채점 화면에 YouGlish 연동(패턴/문장 검색 칩 + 전용 위젯 + 새 탭 링크, 스포일러 방지를 위해 채점 후에만 노출) | app/index.html | "받아쓰기에도 YouGlish로 바로 확인" 요청 |
| 2026-06-13 | 주간 문장 자동 업데이트 파이프라인 신설 — 공개 비즈니스 영어 빈출 표현 기반 N개 생성·중복제거·등록·다국악센트 오디오. Windows 작업 스케줄러 '영어공부-주간문장업데이트'(매주 월 09:00). 소스=공개 자료 기반, YouGlish 검증=앱 위젯 직접 확인(현행), 주기=주1회 자동 (사용자 선택) | app/weekly_update.py 신설 | "비즈니스 영어 빈출 문장 소스에서 지속 업데이트 받고 싶다" 요청 |
| 2026-06-13 | 롤플레이에 '학습 문장 시드' 단계(0) 추가 — 상대역 대사를 사용자의 배운 문장이 자연스러운 응답이 되도록 설계해 대화 곳곳에 배치, 종료 피드백에 '쓴 문장/놓친 문장' 항목 추가 | roleplay-meeting | "내가 배운 문장들을 곳곳에 배치해서 상황극 하고 싶다" 요청 |
| 2026-06-13 | 공식 비즈니스 + 강의/발표 빈출 문장 99개 일괄 등록(s013~s111, 총 111개) + 다국악센트 오디오 792개. scenario에 `meeting`(격식 미팅)·`presentation`(강의/발표) 신설 — 정식 목록(sentence-pack·weekly_update·server.py)에 반영 | app/seed_formal100.py 신설, sentence-pack·app/weekly_update.py·app/server.py 수정 | "공부할 문장이 너무 적다 — 공식 비즈니스/강의 문장 100개 추려서 등록해달라" 요청 |
| 2026-06-13 | 앱에 '상황극' 탭 신설 — 배운 문장을 상대역 대사 곳곳에 배치한 스크립트형 대화 연습(상대 대사 오디오 먼저→내 차례 배운 문장으로 대응→모범발음 재생→이력에 roleplay 기록). 현실성 린트 통과한 시나리오 4종(CRO 결과리뷰·사내 업데이트·CRO 협상·발표 Q&A, 학습문장 26개 배치) | data/scenarios.json·app/seed_scenarios_audio.py 신설, app/index.html 수정 | "공부한 문장 배치해서 상황극 탭 만들어달라 + 현실에서 쓰일 상황인지 린트" 요청 |
| 2026-06-13 | 상황극 자동 확장 + YouGlish 연동: ① 주간 파이프라인이 문장 추가 후 배운 문장을 배치한 시나리오 1개를 자동 생성(generate_scenario.py — en 정확 일치로 ref 매핑해 실제 학습문장만 배치, 상대역 오디오 생성). ② 상황극 '내 차례' 정답 공개 후 YouGlish 위젯·검색 칩(패턴/문장/새탭) 노출 | app/generate_scenario.py 신설, app/weekly_update.py·app/index.html 수정 | "문장 늘면 상황극도 자동 업데이트 + 상황극 문장도 YouGlish로 확인" 요청 |
| 2026-06-13 | 공부 이력 탭에 '공부한 문장' 목록 추가 — 실제 연습한 문장(받아쓰기 시도>0 또는 세션 로그 등장)을 받아쓰기 횟수·통과·SRS 상태·마지막 학습일과 함께 나열, 행 클릭 시 해당 문장 카드로 이동 | app/index.html | "공부 이력에 내가 공부한 문장들을 나열해서 보여달라" 요청 |
| 2026-06-13 | '문장 공부' 탭 제거(받아쓰기와 기능 중복) — nav 버튼·섹션·browse 로직·브라우즈 전용 YouGlish 위젯 삭제. 의존부 재배선: 이력 '공부한 문장' 행 클릭→해당 문장 받아쓰기 시작, 문장 추가 후→카드화면 대신 발음 재생+안내. 받아쓰기 채점화면·상황극의 YouGlish는 유지 | app/index.html | "받아쓰기와 문장공부가 중복 — 문장공부 없애달라" 요청 |
| 2026-06-13 | 오디오 첫 재생 앞부분 잘림 해결 — 원인: 출력 장치(블루투스/HDMI) idle 깨어남 지연. ① 무음 keep-alive(AudioContext)로 출력 스트림 유지(app/index.html). ② 모든 mp3 앞에 ~400ms 무음 프레임 삽입 — generate_audio.py에 prepend_silence 내장(향후 생성분 자동 패딩), 기존 949개는 app/pad_audio.py로 일괄 처리(멱등). edge-tts=24kHz·mono·48kbps CBR MPEG2 L3라 동일 규격 무음 프레임(144B) 이어붙임 | app/index.html, generate_audio.py 수정·app/pad_audio.py 신설 | "첫 재생 때 앞이 뭉개지고 중간부터 들린다" 문의 |
| 2026-06-13 | 받아쓰기 채점 '왜 안 들렸을까' 보강(최종) — 외부 사전 링크 방식은 사용자 피드백으로 폐기. 대신 문장별 study={sound:상세 연음·발음 해설, examples:패턴 응용 예문 3, tip:쓰임} 생성기(app/enrich_study.py, claude -p, 멱등·배치저장) 신설 + 주간 파이프라인 연결. 채점화면을 '👂왜 안 들렸을까(연음·발음, study.sound 우선·없으면 notes)/🗣️이렇게도 써요(예문)/💡쓰임' 블록으로 렌더 | app/index.html·app/enrich_study.py 신설·app/weekly_update.py·sentence-pack 수정 | "사전 링크 말고 연음·발음 설명 더 깊게 + 응용 예문 블록화" 요청 |
| 2026-06-13 | 무료 YouGlish 대체 신설 — 유튜브 자막에서 표현을 찾아 그 타임스탬프부터 임베드 재생. app/yt_clip.py(API키 없이 검색 스크래핑 + youtube-transcript-api로 표현 위치 탐색) + server.py GET /api/ytclip + 받아쓰기 채점·상황극 화면에 '▶ 유튜브에서 이 표현 찾기(무료)' 버튼(findYT). 로컬 서버 전용(온라인은 유튜브 검색 링크 폴백), YouGlish는 보조로 유지. youtube-transcript-api 설치 | app/yt_clip.py·server.py·app/index.html | "YouGlish 무료 아님 — 유튜브에서 문장 찾아 바로 확인하게 만들어달라" 요청 |
| 2026-06-13 | 보강(study)에 '핵심 단어·관용어구(vocab)' 추가 — enrich_study.py 프롬프트·파싱에 vocab[{term,ko}] 추가, 멱등 기준을 vocab 보유로 변경해 전체 재생성. 채점 화면에 '📖 핵심 단어·표현' 블록 렌더 | app/enrich_study.py·app/index.html | "중요한 단어 및 관용어구의 뜻을 알려달라" 요청 |
| 2026-06-13 | YT 영상 찾기 = '패턴' 매칭으로 개선 — 완전 일치 대신 고정 앵커만 맞으면 슬롯(~,...)은 임의 단어 허용(정규식, GAP_WORDS=6), 실패 시 가장 긴 고정 청크로 폴백. 프런트는 원본 패턴(~ 포함) 전송. IP 차단 대응: MAX_VIDEOS=8·요청 간 0.3s·IpBlocked 조기종료+안내·성공결과 캐시. 한계: 무키 자막 스크래핑은 YouTube가 버스트 요청 시 IP 일시 차단(쿠키/프록시로 완화 가능) | app/yt_clip.py·app/index.html | "자막 완전일치 말고 비슷한 패턴이면 찾아달라" 요청 |
| 2026-06-13 | YT 영상 찾기 = 쿠키 인증 + 다중 클립 자동 모음 — youtube_cookies.txt(Netscape) 있으면 requests 세션에 실어 검색·자막 요청(IpBlocked 완화). find_clips가 여러 영상(기본 5) 모아 반환, 앱은 채점·상황극 진입 시 자동 로드해 ◀▶로 유글리쉬처럼 넘겨봄(수동 '찾기' 버튼 제거). 응답에 cookies/clips 포함, 쿠키 안내문(유튜브쿠키_사용법.txt) 신설 | app/yt_clip.py·app/server.py(/api/ytclip n개·find_clips)·app/index.html(loadClips/renderClip/clipMove), 유튜브쿠키_사용법.txt 신설 | "쿠키 방식으로 붙이고, 미리 여러 영상 찾아 넘겨보게 해달라" 요청 |
| 2026-06-13 | '모아두기(📌)' 신설 — 사용자가 직접 고른 문장을 sentences.json의 `collected` 플래그로 표시(로컬 저장, 온라인은 mergeWithLocal로 유지). 진입점 2곳: 받아쓰기 채점화면 토글 버튼·'공부한 문장' 목록 행별 버튼. 공부 이력 탭 상단에 '📌 모아둔 문장' 카드 신설(목록·행별 빼기·'모아둔 N문장 한번에 받아쓰기' 버튼 startCollectedDrill, 전체를 슬라이스 없이 큐로). | app/index.html | "공부한 문장 체크하면 공부 이력으로 모이게 — 나중에 한번에 복습" 요청 |
| 2026-06-14 | 저장된 YouGlish 예문 = 인라인 영상 보기로 변경 — 외부 링크(긴 youtu.be 주소, 새 탭)를 제거하고 '▶ 영상 보기' 칩 버튼으로 교체. 누르면 그 자리(같은 페이지)에 YouTube iframe이 펼쳐져 바로 시청, 다시 누르면 닫힘. 한 번에 하나만 재생(다른 행 열면 기존 것 자동 닫힘). renderYgClips 재구성 + ygPlayInline 신설 | app/index.html | "링크 주소 길어서 안 이쁨 — 주소 빼고, 클릭하면 한 페이지에서 바로 보게 해달라" 요청 |
| 2026-06-14 | 오디오 첫 재생 묵음/keep-alive 처리 원복 — 효과 없다는 사용자 피드백으로 2026-06-13 '앞부분 잘림' 대응을 되돌림. ① 기존 mp3 949개 앞에 박힌 무음 프레임(17×144B=2448B/파일) 전부 제거(역연산 1회 스크립트, 멱등 확인 후 폐기) → 원본 바이트 복원. ② generate_audio.py에서 prepend_silence·_silent_frame·LEAD_MS 제거(원본 상태). ③ index.html의 AudioContext keep-alive(무음 루프 + click 리스너 + playFile 호출) 제거. app/pad_audio.py 삭제 | .claude/skills/multi-accent-audio/scripts/generate_audio.py·app/index.html 수정, app/pad_audio.py 삭제, audio/*.mp3 949개 원복 | "tts 즉시재생 설정이 효과 똑같다 — 처음 버전으로, 앞 묵음도 빼달라" 요청 |
| 2026-06-14 | ① 유튜브 자막 자동찾기(yt_clip) 전면 제거 — app/yt_clip.py·server.py의 /api/ytclip·/api/ytstatus·index.html의 loadClips/renderClip/clipMove·쿠키파일(www.youtube.com_cookies.txt)·안내문(유튜브쿠키_사용법.txt) 삭제(IP차단으로 불안정). ② YouGlish 결과 캐싱 신설 — 위젯 API 이벤트(onVideoChange=영상ID, onCaptionChange=자막문장, onFetchDone=악센트)를 구독해 찾은 예문 `{video,caption,accent,saved}`를 sentences.json의 문장별 `yg_clips`(최대 20개, 영상당 첫=일치 자막 1개, 중복제거)에 자동 저장. 받아쓰기 채점·상황극 화면이 진입 시 저장된 예문(악센트 배지+자막+유튜브 링크)을 먼저 렌더 → 다음엔 YouGlish 하루 횟수 안 쓰고 무료로 재열람. 한계: 위젯 API가 타임스탬프를 안 줘 유튜브 링크는 영상 처음부터. 로컬/온라인(localStorage) 공통 동작 | app/index.html·app/server.py 수정, app/yt_clip.py·쿠키파일·안내문 삭제 | "유튜브 자동찾기는 빼고, YouGlish 찾은 결과를 기억해 다음엔 무료로 다시 보게 해달라" 요청 |
| 2026-06-14 | 저장 예문 목록 정리 + 재생지점 수동입력 — ① renderYgClips에서 URL인코딩된 caption(`%5B%5B%5B…`) 나열 제거(화면 깨짐 원인), '▶ 영상 보기' 버튼만 표시(escHtml 헬퍼도 미사용으로 삭제). ② 재생지점 점프 신설: 위젯 API·유튜브 자막 둘 다로 타임스탬프 자동취득 불가(YouGlish 미노출+동일출처정책, 자막 스크래핑은 IP차단) 확인 → 사용자가 시각을 1회 입력(`예: 1:23`/초)하면 문장별 clip에 `start`(초) 저장, 버튼이 '▶ m:ss부터 보기'로 바뀌고 유튜브 임베드 `&start=`로 그 지점부터 재생. ygParseTime·ygFmtTime·ygSetTime 신설, ygPlayInline 시그니처 (sid,ci,btn)로 변경·라벨 data-label 복원. 로컬/온라인 공통 | app/index.html 수정 | "유글리쉬 저장 링크가 0:00부터 재생돼 의미없음 — 재생지점을 기억해 그 지점부터 보게 해달라" 요청 |
| 2026-06-14 | 받아쓰기 '세션' 제거 — 끊김 없이 지속 학습. ① 큐(8문장) 다 풀면 뜨던 "🎉 세션 완료!" 카드·finishSession() 삭제, nextSentence가 큐 소진 시 refillQueue()로 다음 문장 자동 보충(복습예정 우선·없으면 전체)해 계속 이어감. ② 저장 시점을 문장마다로 이전 — showResult 채점 직후 recordStudy(s,pass) 호출: 그날(today) `mode:dictation` 이력 엔트리를 upsert(todayDictationEntry)해 sentences·attempted·passed 누적 + saveSentences()+study_log 저장. quickAddVocab도 즉시 vocab/log 저장. ③ '그만하기' 버튼(stopDrill)로 언제든 중단(이미 저장됨). ④ 라벨에서 '세션' 제거: 홈 '누적 세션'→'공부한 날'(고유 날짜 수), 이력 '총 세션'→'학습 기록'. per-sentence 통과(pass/fail, SRS)는 유지 | app/index.html 수정 | "세션 없애고 지속적으로 공부 + 공부한 문장 저장 시 날짜만 기록" 요청 |
| 2026-06-15 | 앱 실행 안 됨(포트 충돌) 해결 — 무관한 주식 도구 `signal_report.py`(주식_안드레카파시 wiki 폴더)가 `_find_free_port(8765)`로 8765를 선점하면, 폴백 없는 영어공부 `server.py`가 8765 바인드 실패(WinError 10048)로 죽던 문제. ① 영어공부 앱 전용 포트를 8765→**8770**으로 이전(server.py PORT·docstring, 공부시작.bat 2곳, english-study/SKILL.md). signal_report는 미수정(8765 선호 유지 → 둘이 영구 비충돌). ② server.py 기동부에 OSError 핸들링 추가 — 포트 막힘 시 raw 트레이스백 대신 한글 안내(확인 netstat·대안 포트) 출력. 검증: 8770에서 `/`=302·`/app/index.html`=200·`/data/sentences.json`=200 | app/server.py·공부시작.bat·.claude/skills/english-study/SKILL.md 수정 | "왜 실행이 안될까?" 문의 — 원인=타 프로젝트 프로세스의 8765 점유 |
| 2026-06-14 | 지점(시간) 적은 예문만 영구저장 — 자동수집 클립은 메모리에만 두고(세션 중 후보로 표시) `start` 없는 건 디스크에 안 씀. ① sentencesForSave()/saveSentences() 신설: sentences 저장 시 yg_clips를 `start!=null`만 남긴 사본을 기록(메모리 불변), 모든 sentences 저장경로(applySrs·collected·ygSetTime·add_sentence)를 saveSentences로 통일해 누수 차단. ② saveYgClip은 메모리 추가만(영구저장 제거, 토스트 '🔎 찾음—시간 적어야 저장'). ③ load() 마이그레이션: 기존 무지점 자동수집 클립을 메모리에서 정리. ④ 온라인 모드 yg_clips 미유지 버그 수정 — mergeWithLocal이 localStorage의 yg_clips를 복원하도록 추가 | app/index.html 수정 | "지점 저장한 영상만 기록하고, 지점 저장 안 한 건 저장 안 되게 해달라" 요청 |
